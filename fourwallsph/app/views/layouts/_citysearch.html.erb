    <li class="dropdown">
         <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Search <span class="caret"></span></a>
             <ul class="dropdown-menu">
             <li class="navbar-text ">Search by City</li>
             <% City.all.each do |city| %>
   			     <li> <%= link_to city.name, posts_path(city: city.name) %></li>
    			   <% end %> 
			       <li> <%= render 'layouts/titlesearch' %>
		         <li class="navbar-text "><%= link_to "Advanced Search", new_search_path %></li>

    	       </ul>

             </li>